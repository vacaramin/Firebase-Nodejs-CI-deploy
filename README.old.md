# Firebase-Nodejs-CI-deploy
A repository containing Node.js code for a Firebase-hosted application along with configuration for continuous integration (CI) and continuous deployment pipeline automation.
